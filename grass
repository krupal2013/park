This is first arg $1 and this second arg $2

if [ $1 == grass ]
then
git add $1
git commit -m "$2"
git remote add mygithub git@krupalgithub:krupal2013/park.git
git push mygithub

else
 	echo error
fi

